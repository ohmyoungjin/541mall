spring:
  datasource:
    #url: jdbc:h2:tcp://localhost/~/541mall
    url: jdbc:mysql://localhost:3306/mall_api?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: maeng2
    password: 1234
    #driver-class-name: org.h2.Driver
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #show_sql: true
        format_sql: true

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace


